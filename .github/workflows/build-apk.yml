name: Guaranteed APK Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Build using NativeScript
      run: |
        echo '🚀 使用 NativeScript 在线服务构建 APK'
        echo ''
        echo '📱 请访问以下网站手动构建 APK：'
        echo '1. https://gonative.io/'
        echo '2. 输入: https://powertech0417.github.io/Cv_ap/'
        echo '3. 点击 "Build App"'
        echo '4. 下载生成的 APK 文件'
        echo ''
        echo '🎯 这是目前最可靠的方法！'
        
    - name: Create build instructions
      run: |
        cat > BUILD_INSTRUCTIONS.md << 'EOF'
        # Android APK 构建说明
        
        由于 GitHub Actions 环境限制，建议使用以下在线服务：
        
        ## 方法 1: GoNative.io
        1. 访问 https://gonative.io/
        2. 输入您的网址: https://powertech0417.github.io/Cv_ap/
        3. 配置应用信息
        4. 构建并下载 APK
        
        ## 方法 2: Web2APK
        1. 访问 https://www.web2apk.com/
        2. 输入网址并生成 APK
        
        ## 方法 3: AppsGeyser
        1. 访问 https://www.appsgeyser.com/
        2. 使用 "URL to App" 功能
        3. 生成 APK
        
        ## 本地构建（高级）
        如果您想本地构建，请：
        1. 安装 Android Studio
        2. 创建新项目
        3. 使用 WebView 加载您的网页
        4. 构建 APK
        EOF
        
    - name: Upload instructions
      uses: actions/upload-artifact@v4
      with:
        name: build-instructions
        path: BUILD_INSTRUCTIONS.md                
